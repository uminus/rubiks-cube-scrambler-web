{
  "name": "scrumbler",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "npm run --workspaces build --if-present",
    "start": "node ./packages/backend/dist/app.js",
    "dev": "pnpm dev --filter ."
  },
  "workspaces": [
    "packages/*"
  ]
}
